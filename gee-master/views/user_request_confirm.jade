extends layout
block pageHeader
	div.pageHeader Account requested for User #{email}
block content
	div An account has been requested for User #{email} with name #{name}, with comments attached:
	div #{comments}