# Sample commands:
# ansible nodes -i ansible-hosts --private-key id_rsa -u root -m ping
# ansible nodes -i ansible-hosts --private-key id_rsa -u root -m shell -a "uptime"

[nodes]
{% for host in groups['nodes'] %}
{{ slice }}.{{ host }} ansible_ssh_host={{ hostvars[host]['ansible_eth0']['ipv4']['address'] }} ansible_ssh_port={{ hostvars[host]['docker_containers'][0]['NetworkSettings']['Ports']['22/tcp'][0]['HostPort'] }}
{% endfor %}
